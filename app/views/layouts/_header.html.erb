<%# yield(:page) %>
<nav>

  <ul>
    <li><%= link_to "All articles", articles_path %></li>
    <li><%= link_to "Josh Weaver", user_path(1) %></li>
    <li><%= link_to "Aaron Augello", user_path(2) %></li>
    <li>not a link</li>
    
    <% unless params[:controller] == "articles" && params[:action] == "index" #don't show on Articles#index %> 
    	<li>
    		<%= link_to "New Article", new_user_article_path(@user) %>
    	</li>
	<% end %>
  
  </ul>

</nav>