<h1>Home Page</h1>

<% @articles.each do |article| %>
	<div>
		<span><%= article.title %></span>
		<empahsis>- by <%= article.user.name %></emphasis>
		
		<%= render 'layouts/articleControls', article_id: article.id %>

	</div>
<% end %>